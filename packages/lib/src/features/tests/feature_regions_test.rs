println!(
    "{:?}",
    compute_regions(Some(0.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(10.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(20.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(30.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(40.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(50.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(60.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(70.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(80.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(90.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
println!(
    "{:?}",
    compute_regions(Some(100.0), 0.0, 100.0, 30.0, 70.0).unwrap()
);
