#![allow(
    clippy::needless_return,
    clippy::type_complexity,
    clippy::needless_range_loop,
    unused
)]

mod base;
mod data;
mod utils;

fn main() {
    // let mut snapshot = ComponentTestSnapshot::<bool>::new();
    // snapshot.debug_mode_max(2);
    // snapshot.push(Some(true));
    // snapshot.push(Some(true));
    // snapshot.push(Some(true));
    // snapshot.push(Some(true));
    // snapshot.push(Some(true));
    // snapshot.push(Some(true));
    // snapshot.assert(&[
    //     Some(true),
    //     Some(true),
    //     Some(true),
    //     Some(true),
    //     Some(true),
    //     Some(true),
    // ]);
    // println!("xdd")
}
